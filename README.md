"# E71a_FakePartnerStudy" 
This is to analyse partner tracks in the final stage.





git add mac
git commit -m "first commit of root macro"
git push

git add bat
git commit -m "first commit of batchfile for data"
git push

git add bat_comp_data_MCfake
git commit -m "first commit of batchfile for data"
git push

git add MC_bat
git commit -m "first commit of batchfile for MC"
git push
